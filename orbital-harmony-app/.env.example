# Public API URL only. This is safe to embed and is required for iOS builds
# because `orbital://app/api/trivia` would otherwise resolve as a bundled file.
# Web deployments use same-origin /api/trivia when this is left blank.
VITE_TRIVIA_API_URL=https://your-app.example/api/trivia